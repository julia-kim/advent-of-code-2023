import { part1 } from './part1';
import { part2 } from './part2';

describe('day XX - part 1', () => {
  it('does the thing', () => {
    const input = ['1', '2', '3'];
    const result = part1(input);
    expect(result).toBe('1');
  });
});

describe('day XX - part 2', () => {
  it('does the other thing', () => {
    const input = ['1', '2', '3'];
    const result = part2(input);
    expect(result).toBe('1');
  });
});
